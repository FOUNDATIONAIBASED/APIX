<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg">

    <!-- ZXing Embedded scanner view fills most of the screen -->
    <com.journeyapps.barcodescanner.DecoratedBarcodeView
        android:id="@+id/barcodeView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        app:zxing_scanner_layout="@layout/zxing_capture" />

    <TextView
        android:id="@+id/tvInstruction"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="20dp"
        android:gravity="center"
        android:textSize="14sp"
        android:lineSpacingMultiplier="1.4"
        android:textColor="@color/text_primary"
        android:text="Point camera at the QR code shown in\nthe ApiX Gateway management console → Devices" />

    <Button
        android:id="@+id/btnManualEntry"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginHorizontal="16dp"
        android:layout_marginBottom="16dp"
        android:text="@string/qr_use_manual_setup"
        android:textColor="@color/text_secondary"
        android:background="@drawable/bg_btn_ghost"
        android:fontFamily="monospace"
        android:textSize="12sp" />

</LinearLayout>
